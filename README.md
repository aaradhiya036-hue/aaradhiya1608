# aaradhiya — Cherry Pop

## Photos
Your uploaded photos are already included in `images/` and wired into the scrapbook:
- friends.jpg
- trio.jpg
- pens.jpg
- study.jpg
- coffee.jpg

## Deploy
Upload the whole folder to Vercel. Keep the `images` folder exactly where it is.

## Letters
`letter.html` + `letter.js` submit messages to the Firebase Firestore `letters` collection using the Firebase project configuration you provided.

If Firestore rejects submissions, publish the included `firestore.rules` in Firebase Console.
