# aaradhiya — Cherry Pop / Scrapbook

Deploy the entire folder to Vercel.

- `index.html` — redesigned scrapbook homepage
- `letter.html` + `letter.js` — letter page (kept unchanged)
- `style.css` — base site styles
- `script.js` — mood + mayhem interactions
- `favicon.svg` — matching thin red star favicon
- `images/` — the five uploaded photos
- `firestore.rules` — Firestore rules for the letters collection

The homepage cursor leaves small thin red outlined stars instead of cherries.
